# To-do

最好添加到 wing 里的一些 web 组件：
- db component (db driver with pool, ORM/AR) (like ADO.NET and  EF Core, or db and AR in yii2)
- cookie component (read and write header, better developer experience)
- session middleware (session interface, and different storage: file, redis)
- authentication middleware (basic, bearer, cookie, header, query)
- authorization middleware (acl, role, claim)
- user component (user identity, auth status, claims, request context)
- logging component (collect logs and redirect to different targets: file, db)
- cache component (cache interface, and different underlying drivers: memory / file / redis / memcache)
- queue component or async job component (yii2 queue, or hangfire in .net)
- compress component (gzip, zstd, brotli for better response compression)
- view component (template engine, e.g. php twig, razor in asp.net core)
- i18n component
- websocket
- sse